so far : 
    - fix the responese of GET Customers = return customer : [{ id , full_name , phone_number , total_debts , paid_amount , rmaining_amount  }]
    - Create the payments endpoint to GET submited payments
    - Fix the account/my_shops = change the endpoint to account/me 
    - Fix the responese of account/me = return { total_amount , number_of_debts , shops }
    - Fix the responese of account/me .shops = return { shop_id , total_amount , number_of_debts , shop_address , shop_name }
    - Create a endpoint named modal whit url api/modal?type={ ANYTHING }
        > api/modal?type=customers and it returns = [{ id , phone_number , full_name }]
        > api/modal?type=products and it returns = [{ id , name , barcode , sell_price }]
        > api/modal?type=credits and it returns = { ok , debts : [{ id , remaining , created_at , is_paid }] }
    - Create the products search by name and barcode and category
    - add the django silk by pip install django-silk
todo : 
    + Change the responese of /api/debts/ to => {ok , debts : [{ id , customer_name , total_amount , paid_amount , rmaining_amount , created_at , is_paid }]}
        dont send the whole data of debts just these 
    + Change the responese of /api/sales/ to => {ok , sales : [{id , customer_name , total , created_at}]}
    + Change the responese of /api/products/ to => {ok , products: [{id , name , barcode , sell_price , created_at , stock }]}
    + Create a api for dashboard data /api/dashboard/ and return responese => {ok , data : { total_debts_price , total_sales_price , total_price , total_payed_amount , number_of_customers }}